spring.application.name=project

# PostgreSQL Database Connection Settings
spring.datasource.url=jdbc:postgresql://localhost:5432/SecureEmployeesManagement
spring.datasource.username=postgres
spring.datasource.password=tharayelaymaaloe145
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Spring Data JPA Settings (Flyway သုံးထားလို့ ddl-auto ကို validate ပဲ ထားပါမယ်)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8083/realms/secure-employee-management
